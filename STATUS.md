# TallyMark status

## Current implementation unit

PR13 — TaskConnect integration merged as pull request #31; [GitHub issue #30](https://github.com/suporterfid/tallymark/issues/30) is closed. It adds an independently disabled TaskConnect outbox after aggregate commit, one active task per goal/UTC-hour per aggregation tick with a stable idempotency key, aggregate-only conversion bodies, direct digest-delegation seam, TaskConnect API-key or GrandpaSSOn `tasks:write` service-token authentication, and an authenticated platform health endpoint. The collector is unchanged. Review follow-up added tick-level aggregation, transactional claims, bounded exponential retry backoff, and platform-only local access to instance-wide health metrics. Before merge, Pint passed on changed PHP files; after merge on `main`, `./scripts/tm.sh test` passed 100 tests / 583 assertions. The closed issue records the verified scope and collector/dependency checks.

PR12 â€” GrandpaSSOn seam merged as pull request #29; GitHub issue #28 is closed. It adds independently disabled browser and machine-token modes, distinct confidential-RP and service-client configuration, immediate HTTP `/session/exchange` redemption with state binding, local-user provisioning by e-mail, active-tenant-only group-to-role membership synchronization with broker-managed revocation, form-body introspection credentials, and SHA-256-fingerprint cache entries bounded by the broker `exp`. The API chain validates active `gpat_live_` tokens, the method's required scope, and raw or prefixed tenant audience; its denials are tenant-audited by fingerprint only. The standalone collector remains unchanged. On merged `main`, `./scripts/tm.sh test` passed 93 tests / 556 assertions; focused tests cover flags-off defaults, both audience forms, body credentials without Basic auth, cache expiry, state mismatch/expiry failure, local login, tenant isolation, machine-token denial audit, and an `analytics:write` mutation through the Gate. Independent review found no P0/P1 blockers.

PR11 â€” Goals & shared dashboards merged as pull request #27; GitHub issue #26 is closed. Owner authorization resolved open question 10: goals use exactly one event-name or literal URL-prefix matcher; the public Blade view shows only all-time aggregate pageviews, sessions, and conversions without goal names; the opaque `dash_` token can be enabled, disabled, and rotated; rendered output is cached for five minutes. On merged `main`, `./scripts/tm.sh test` passed 77 tests / 502 assertions, and independent review found no Critical or Important blockers.

PR3 — Standalone collector merged as pull request #8; GitHub issue #7 is closed. PR4 — Visitor hashing & salts merged as pull request #10; GitHub issue #9 is closed. PR5 — Ingest pipeline merged as pull request #12; GitHub issue #11 is closed. PR6 — Sessionization & classification merged as pull request #14; GitHub issue #13 is closed.

PR3 adds a standalone `public/px.php` with no Composer, Laravel, or database path. Isolated bare-server tests verify 204/CORS, no cookie, unknown-key drops, host validation, DNT, bots, body caps, IPv4/IPv6 and raw-user-agent suppression, URL sanitization, concurrent shard caps, GIF fallback, and p99 PHP timing. On merged `main`, `./scripts/tm.sh test` passed 16 tests / 183 assertions; Composer validation and the CI Compose configuration also passed before merge.

PR4 adds SHA-256 64-bit daily visitor hashes, a UTC-midnight salt rotation, one-hour salt destruction grace, a persistent `system_heartbeats` alarm for missed rotations, and the current salt to the atomically-generated collector map. It also removes request IP/User-Agent storage from fresh and existing session schemas and fixes the session driver to files. On merged `main`, `./scripts/tm.sh test` passed 22 tests / 228 assertions; Composer validation and the CI Compose configuration also passed before merge.

PR5 adds `analytics:ingest`, closed-buffer ordering, token-owned claim leases, bounded resumable line checkpoints, malformed-line tolerance, and a transient sanitized `ingest_events` stage committed before buffer deletion. On merged `main`, `./scripts/tm.sh test` passed 29 tests / 257 assertions, including exactly-once, active/reclaimed lease, budget, resume, collector-contract, and IPv4/IPv6 staging-privacy coverage; Composer validation and the CI Compose configuration also passed before merge.

PR6 uses the owner-authorized resolution of open question 8: a zero-dependency collector classifier writes only derived bot/device/browser/OS fields. `Sessionizer`, `ReferrerNormalizer`, the internal classifier, an application-only MIT Public Suffix List parser, a versioned MPL-2.0 suffix list, and the runtime licence audit are implemented. On merged `main`, `./scripts/tm.sh test` passed 43 tests / 303 assertions; the Docker-run licence audit and strict Composer validation also passed.

PR7 — Aggregation & cardinality merged as pull request #16; GitHub issue #15 is closed. It adds all hourly dimensions, unique keys, idempotent staged-batch aggregation, a pure session-state transition with persistent cross-batch correction, cardinality folding, self-referral normalization, and durable cap warnings. On the reviewed branch, `./scripts/tm.sh test` passed 55 tests / 347 assertions and the Docker-run runtime licence audit passed; final review found no blockers.

PR8 — Rollups & retention merged as pull request #18; GitHub issue #17 is closed. It adds daily aggregate tables, exact `daily_visitors` materialized from the closed daily session state, idempotent `analytics:rollup`, and chunked retention that deletes hourly rows only after their matching daily rollup exists. On the reviewed branch, `./scripts/tm.sh test` passed 57 tests / 356 assertions and the Docker-run runtime licence audit passed; final review found no blockers.

PR9 — Dashboard API & SPA merged as pull request #21; GitHub issue #19 is closed. It adds bounded reporting queries over hourly and exact daily aggregates, explicitly labelled prior-period comparison precision, dimension breakdowns, minimum event-goal conversion counts/rates with daily rollup/retention, and a five-minute aggregate powering the honestly labelled current last-30-minutes report. The authenticated Vue/TypeScript SPA includes all §19 navigation screens with complete `en`/`pt-BR` text, accessible table alternatives, site-timezone rendering, documented ingestion lag, and visible ingest-freshness, shed-event, and cardinality-cap operational warnings. On merged `main`, `./scripts/tm.sh test` passed 67 tests / 413 assertions; `vue-tsc`, the Vite build, targeted Pint, and the Docker-run runtime licence audit also passed. Final review found no P0/P1 issues.

Security hotfix — Composer advisory remediation merged as pull request #23; GitHub issue #22 is closed. It upgrades transitive MIT-licensed `guzzlehttp/guzzle` from 7.15.1 to 7.15.2, resolving the documented high and medium advisories, and adds a fail-closed, three-attempt network-backed Composer audit before release builds. On merged `main`, `./scripts/tm.sh test` passed 68 tests / 421 assertions; Composer strict validation and the Docker runtime licence audit passed. The update's network-backed audit reported no advisories; a later Packagist timeout was correctly treated as unverifiable by the no-cache release gate rather than as a pass.

PR10 â€” Tracking script merged as pull request #25; GitHub issue #24 is closed. It adds a dependency-free `public/tm.js` compatibility loader plus a content-hashed hand-written tracker, immutable cache handling, CSP installation guidance, browser privacy guards, query-allowlisted payload URLs, silent beacon/fetch/image delivery fallbacks, custom events, and opt-in SPA history tracking. `public/px.php` is unchanged. On merged `main`, the targeted tracker contract passed 3 tests / 45 assertions, the full `./scripts/tm.sh test` suite passed 71 tests / 466 assertions, Apache served both tracker paths with `Cache-Control: public, max-age=31536000, immutable`, and independent review found no Critical or Important blockers.

## Licence decision

The specification was amended with owner authorization: a dual-licensed dependency is acceptable when it offers a permitted option, TallyMark selects that option, and the selection is recorded from its authoritative licence file. `nette/schema` and `nette/utils` are used under their New BSD option; the audit record is in `docs/security/dependency-audit.md`.

GPL-only, LGPL-only, AGPL-only, and SSPL-only dependencies remain prohibited. The executable release audit is scheduled for PR14; the selected Nette licence options are covered by a regression test now.
